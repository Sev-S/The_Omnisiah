#include <string>

class Person {
private:
    std::string name;
public:
    Person(std::string name) : name(name) {};
    std::string get_name() {
        return name;
    };
    bool check_
};

Person realElvis(std::string elvis);
Person fakeElvis(std::string elvis);